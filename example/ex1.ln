// these arent two arguments, they are curried
fn add (a:Int b:Int) -> Int = a + b

// effect tracking will come later, for now it is IO monad
fn main() -> Eff Unit = {
  print $ "Hello world
  
  loop (n = 0) { 
    if n < 10 { 
      print (to_string n)
      next (n + 1)
    } else ()
  } 
}


